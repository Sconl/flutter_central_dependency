name: flutter_central_dependency
description: "Shared dependencies for Flutter projects."
author: Sconl Peter | Ace Cyber Space

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  animated_splash_screen: ^1.3.2  # Updated to latest version
  animated_text_kit: ^4.3.0  # Updated to latest version
  auto_size_text: ^3.0.1  # Updated to latest version
  badges: ^3.2.0  # Updated to latest version
  bloc: ^8.1.0  # Consider adjusting constraint based on needs
  cached_network_image: ^3.4.2  # Updated to latest version
  cloud_firestore: ^4.17.0  # Updated to latest version
  cloud_firestore_platform_interface: ^6.2.2  # Updated to latest version
  cloud_firestore_web: ^3.11.4  # Updated to latest version
  collection: ^1.18.0  # Consider updating if needed
  equatable: ^2.0.5  # Updated to latest version
  firebase_auth: ^4.17.9  # Updated to latest version
  firebase_core: ^2.2.5  # Updated to latest version
  firebase_core_platform_interface: ^6.3.1  # Updated to latest version
  firebase_core_web: ^2.12.1  # Updated to latest version
  flutter:
    sdk: flutter
  flutter_animate: ^4.5.2  # Updated to latest version
  flutter_bloc: ^8.1.0  # Updated to latest version (consider adjusting constraint)
  flutter_cache_manager: ^3.3.2  # Updated to latest version
  flutter_svg: ^2.2.0  # Updated to latest version
  font_awesome_flutter: ^11.0.0  # Updated to latest version
  from_css_color: ^2.0.0  # Consider updating if needed
  go_router: ^13.3.1  # Updated to latest version
  google_fonts: ^6.2.1  # Updated to latest version
  google_sign_in: ^5.3.0  # Updated to latest version
  intl: ^0.19.4  # Updated to latest version
  json_path: ^0.7.0  # Consider updating if needed
  lottie: ^4.0.0  # Updated to major version (consider compatibility)
  material_design_icons_flutter: ^8.1.0  # Updated to latest version
  page_transition: ^2.2.0  # Updated to latest version
  path_provider: ^2.2.8  # Updated to latest version
  path_provider_android: ^2.2.3  # Updated to latest version
  path_provider_foundation: ^2.3.7  # Updated to latest version
  path_provider_platform_interface: ^2.2.1  # Updated to latest version
  plugin_platform_interface: ^2.2.0  # Updated to latest version
  provider: ^6.1.2  # Updated to latest version
  rive: ^0.14.0  # Updated to latest version
  shared_preferences: ^2.3.1  # Updated to latest version
  smooth_page_indicator: ^2.0.1  # Updated to major version (consider compatibility)
  sqflite: ^2.3.2  # Consider updating if needed
  stream_transform: ^2.2.0  # Updated to latest version
  timeago: ^3.6.2  # Updated to latest version
  url_launcher: ^6.3.1  # Updated to latest version
  url_launcher_android: ^6.4.3  # Updated to latest version
  url_launcher_ios: ^6.3.2  # Updated to latest version
  url_launcher_platform_interface: ^2.3.2

dev_dependencies:
  flutter_lints: ^3.0.1
  lints: ^3.0.0
  flutter_test:
    sdk: flutter
  flame_test: ^1.17.3  #
