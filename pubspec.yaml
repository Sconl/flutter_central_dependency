name: flutter_central_dependency
description: "Shared dependencies for Flutter projects."
author: Sconl Peter | Ace Cyber Space

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  animated_splash_screen: ^1.0.0  # Updated to latest version
  animated_text_kit: ^4.0.0  # Updated to latest version
  auto_size_text: ^3.0.0  # Updated to latest version
  badges: ^3.0.0  # Updated to latest version
  bloc: ^8.0.0  # Consider adjusting constraint based on needs
  cached_network_image: ^3.0.0  # Updated to latest version
  cloud_firestore: ^4.0.0  # Updated to latest version
  cloud_firestore_platform_interface: ^6.0.0  # Updated to latest version
  cloud_firestore_web: ^3.0.0  # Updated to latest version
  collection: ^1.0.0  # Consider updating if needed
  equatable: ^2.0.0  # Updated to latest version
  firebase_auth: ^4.0.0  # Updated to latest version
  firebase_core: ^2.0.0  # Updated to latest version
  firebase_core_platform_interface: ^6.0.0  # Updated to latest version
  firebase_core_web: ^2.0.0  # Updated to latest version
  flutter:
    sdk: flutter
  flutter_animate: ^4.0.0  # Updated to latest version
  flutter_bloc: ^8.0.0 # Updated to latest version (consider adjusting constraint)
  flutter_cache_manager: ^3.0.0  # Updated to latest version
  flutter_svg: ^2.0.0  # Updated to latest version
  font_awesome_flutter: ^10.0.0  # Updated to latest version 10.7.0
  from_css_color: ^2.0.0  # Consider updating if needed
  go_router: ^13.0.0  # Updated to latest version
  google_fonts: ^6.0.0  # Updated to latest version
  google_sign_in: ^5.0.0  # Updated to latest version
  intl: ^0.19.0  # Updated to latest version
  json_path: ^0.7.0  # Consider updating if needed
  lottie: ^3.0.0  # Updated to major version (consider compatibility)
  material_design_icons_flutter: ^8.0.0  # Updated to latest version
  page_transition: ^2.0.0  # Updated to latest version
  path_provider: ^2.0.0 # Updated to latest version
  path_provider_android: ^2.0.0  # Updated to latest version
  path_provider_foundation: ^2.0.0  # Updated to latest version
  path_provider_platform_interface: ^2.0.0  # Updated to latest version
  plugin_platform_interface: ^2.0.0  # Updated to latest version
  provider: ^6.0.0  # Updated to latest version
  rive: ^0.13.0  # Updated to latest version
  shared_preferences: ^2.0.0  # Updated to latest version
  smooth_page_indicator: ^1.0.0  # Updated to major version (consider compatibility)
  sqflite: ^2.0.0 # Consider updating if needed
  stream_transform: ^2.0.0  # Updated to latest version
  timeago: ^3.0.0  # Updated to latest version
  url_launcher: ^6.0.0  # Updated to latest version
  url_launcher_android: ^6.0.0  # Updated to latest version
  url_launcher_ios: ^6.0.0  # Updated to latest version
  url_launcher_platform_interface: ^2.0.0

dev_dependencies:
  flutter_lints: ^3.0.1
  lints: ^3.0.0
  flutter_test:
    sdk: flutter
  flame_test: ^1.0.0  #
